{
  "_comment": "Entries are case-insensitive substring matches against task name, source, command path, and command text.",
  "allowlist": [
    "\\Microsoft\\Windows\\Defrag\\ScheduledDefrag",
    "/etc/cron.daily/logrotate",
    "known-maintenance-task"
  ]
}
